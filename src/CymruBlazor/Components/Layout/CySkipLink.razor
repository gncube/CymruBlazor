@inherits CyLayoutComponentBase

<a
    id="@Id"
    class="@CssClass"
    style="@CssStyle"
    href="@($"#{TargetId}")"
    @attributes="AdditionalAttributes">

    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        @DefaultText
    }

</a>
