namespace CymruBlazor.Icons;

/// <summary>
/// Built-in icon glyphs. CymruBlazor ships a small, purpose-picked set rather than a full
/// icon library (see Spec.md §11 — a dedicated CymruBlazor.Icons package is future work).
/// Consuming apps can render their own SVGs directly where a glyph isn't listed here.
/// </summary>
public enum CyIconName
{
    ChevronDown,
    Info,
    ArrowRight,
    Stethoscope,
    ArrowUpDown,
    Watchlist,
    Bed,
    UserPlus,
    FileText,
    Plus,
    Activity,
    Sun,
    Moon,
    Menu,
    Close
}
